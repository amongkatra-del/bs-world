# bs.world v0.8
Integrated fixes: real chat list from Supabase, automatic incoming-call routing while the app is open, atomic message read helper, and queued ICE candidate flushing after remote description.

Calling still requires a native Android development build and TURN for reliable worldwide NAT traversal; these are not safely faked in client code.
